import { LAYOUT_WIDTH, LAYOUT_HEIGHT, COLORS } from "../config.js";

export class FinalDescPage extends Page {
    constructor(slide, data = {}) {
        this.slide = slide;
        this.data = data;
    }

    render() {

    }
}